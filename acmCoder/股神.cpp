#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <queue>
#include <algorithm>
using namespace std;

void func(int k) {
    int val = 1, cnt = 1, tmp;
    for(int i = 0; i < k; ++i) {
        if(cnt)
    }
}

int main() {
    int n;
    int cnt = 0;
    int val = 1;
    while(~scanf("%d", &n)) {

    }
}