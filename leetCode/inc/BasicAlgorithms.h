#ifndef _BASE_ALGORITHMS__H_
#include "stdafx.h"
namespace Solution {

    int binarySearch(const vector<int>& nums, int start, int end/*real end idx 's next*/, int target);
    int targetSearch(const vector<int>& nums, int target);
}

#endif // !_BASE_ALGORITHMS__H_
